{
  "name": "chronicle-bot-button-smoke",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright-driven smoke of the Discord /record + consent-button flow.",
  "scripts": {
    "install-browsers": "playwright install chromium",
    "login": "playwright test --project=login --headed --timeout=600000",
    "smoke": "playwright test --project=smoke",
    "smoke:headed": "playwright test --project=smoke --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "typescript": "^5.4.0"
  }
}
